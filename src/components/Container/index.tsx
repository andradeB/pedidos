import styled from 'styled-components/native';
import {
    space,
    borderRadius,
    color,
    background,
    alignItems,
} from 'styled-system';

const Container = styled.View`
  ${space};
  ${background};
  ${color};
  ${borderRadius};
  ${alignItems};
`;

export default Container;
